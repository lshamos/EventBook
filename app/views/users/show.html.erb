<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1><%= @user.name %></h1>
    <h2><%= link_to 'Add an Event', new_events_path %></h2>

    <ul>
      <% @user.events.each do |e| %>
        <li><%= link_to e.name, event_path(e) %></li>
          <% end %>
    </ul>  
  </div>
</div>
